db:
  image: mysql
  environment:
    MYSQL_ROOT_PASSWORD: password
  expose:
    - "3306"
  volumes_from:
    - data

web:
  build: .
  volumes:
    - .:/usr/src/app
  ports:
    - "3000"
  links:
    - db

data:
  image: ubuntu
  command: "true"
  volumes:
    - /var/lib/mysql
